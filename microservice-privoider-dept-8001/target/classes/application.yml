server:
  port: 8001
mybatis:
  config-location: classpath:mybatis/mybatis.cfg.xml
  type-aliases-package: com.ldw.microservice.entity
  mapper-locations: mybatis/mapper/**/*.xml
spring:
  application:
    name: microservice-dept
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/huobi_pool?serverTimezone=UTC
    username: root
    password: aaaaa888
  devtools:
    restart:
      enabled: true    #设置开启热部署
      additional-paths: src/main/java #重启目录
  boot:
    admin:
      client:
        url: http://localhost:8788
eureka:
  client:
    service-url:
      defaultZone: http://192.168.171.130:7001/eureka/
#    fetch-registry: true
  instance:
    instance-id: microservice_dept_8002
    prefer-ip-address: true
info:
  app.name: microservice
  company.name: www.ldw.com
  build.artifactId: $project.artifactId$
  build.version: $project.version$

management:
  endpoints:
    web:
      exposure:
        include=*: